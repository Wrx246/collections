import { Box } from '@mui/material'
import { Panel } from '../modules/admin'

const Admin = () => {
    return (
        <Box sx={{ width: '80%' }}>
            <Panel />
        </Box>
    )
}

export default Admin